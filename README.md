# 😎 AI นักข่าวจำลอง (AI News Reporter)

นี่คือโปรเจกต์แชทบอท AI ที่สร้างด้วย Python, Gradio, และ Google Gemini โปรแกรมมีความสามารถในการดึงข่าวสารล่าสุดจาก RSS Feed (เช่น Thairath) แบบเรียลไทม์ และใช้ `news-please` เพื่อสกัดเนื้อหาข่าวฉบับเต็มมาใช้เป็นบริบทในการตอบคำถาม

## ✨ คุณสมบัติ (Features)

* **UI โต้ตอบ:** ใช้งานง่ายผ่านหน้าเว็บด้วย Gradio ChatInterface
* **เชื่อมต่อข่าวสด:** ดึงข่าวล่าสุด 3 รายการจาก RSS Feed (feedparser)
* **สกัดเนื้อหาเต็ม:** ใช้ `news-please` และ `newspaper4k[th]` เพื่อดึงเนื้อหาข่าวฉบับเต็ม (รองรับภาษาไทย)
* **โมเดล AI:** ใช้ Google Gemini ในการสรุปและตอบคำถามจากบริบทข่าวที่ดึงมา

## 🚀 คู่มือการติดตั้งและใช้งาน (Setup & Run)

ทำตามขั้นตอนเหล่านี้เพื่อรันโปรเจกต์บนเครื่องของคุณ

### 1. โคลน (Clone) Repository
ดาวน์โหลดโปรเจกต์นี้ลงในเครื่องของคุณ
```
git clone https://github.com/Mike0165115321/Realtime-News.git

cd Realtime-News
```
### 2. สร้างและเปิดใช้งาน Virtual Environment
(ควรทำนะครับ) เพื่อป้องกันไม่ให้ Library ตีกันกับโปรเจกต์อื่น

# สร้างโฟลเดอร์ .venv
```
python -m venv .venv

# เปิดใช้งาน (สำหรับ Linux/Mac)
source .venv/bin/activate

# เปิดใช้งาน (สำหรับ Windows)
.\.venv\Scripts\activate

```

### 3. ติดตั้ง Library ที่จำเป็น
ติดตั้ง Library ทั้งหมดที่โปรเจกต์นี้ต้องการ ผ่านไฟล์ requirements.txt

```
pip install -r requirements.txt

```

### 4. ตั้งค่า API Key (สำคัญที่สุด)
โปรเจกต์นี้จำเป็นต้องใช้ API Key จาก Google AI Studio เพื่อเรียกใช้ Gemini

ไปที่ Google AI Studio เพื่อสร้าง API Key (หากยังไม่มี)
```
https://aistudio.google.com/api-keys

```

### เปิดไฟล์ SR-AI.py 

มองหาบรรทัดนี้:
```
genai.configure(api_key="GOOGLE_API_KEY")
# คีย์โมเดล gemini...
แก้ไข "GOOGLE_API_KEY" ให้เป็น Key จริง ที่นักเรียนคัดลอกมา (เช่น "AIzaSy...xxx")

```

### 5. รันโปรแกรม
หลังจากตั้งค่าทุกอย่างเรียบร้อย สั่งรันโปรแกรม:
```
python SR-AI.py

```
### 6. เปิดใช้งาน
โปรแกรมจะแสดงผลใน Terminal ของคุณว่า:
```
* Running on local URL: http://127.0.0.1:7860

```